package CommonCards;

import java.util.ArrayList;
import java.util.List;

import Program.Suit;

public class Deck extends Card{

	private static String rank;
	private static Suit suit;
	private CommonCards.Card cardFromDeck;
	private int cardsLeft;


	public <DealCard> Deck() {
		super(Rank rank, Suit suit);
		this.card = card;
		
		int CardsLeft cardsLeft = List<Card> - 1;
		DealCard cardFromDeck;
		
		List<Card> card  = new ArrayList.Card(52);
		for (int i = 0; i < ArrayList.length; i++) {
//			CommonCards.Card[] cards;
//			Card c = cards[i];
			String name = c.toString();
		}
		public int checkDeckSize() {
			return cardsLeft;
		}
//Need to remove each card delt from the deck
		public Card dealCard() {
			return cardFromDeck;
		}
	
//Need to shuffle cards and print
	private void shuffle() {
		
	}
	
}
